- **DL-C：Deep Learning 通信与系统集成负责人**
  - 核心职责
    - 负责树莓派主控程序、USB 串口通信、任务状态机、软件模块通信、统一日志和端到端集成。
    - 负责把视觉模型输出、巡线结果和 Arduino 状态组合成可执行的机器人任务流程。
    - 负责组织跨组接口冻结、联调、降级决策和最终演示流程。
  - 7 月 19 日至 7 月 20 日
    - 与 RB-C 定义并冻结树莓派和 Arduino 之间的 USB 串口协议。
    - 定义 `CMD`、`STOP`、`ACT` 和 `STATE` 消息的字段、单位、序号及错误处理方式。
    - 编写树莓派串口主机端，支持速度命令、停止命令、执行器命令和 Arduino 状态接收。
    - 建立模拟串口和模拟视觉事件，使状态机开发不依赖真实硬件持续在线。
    - 验证 115200 bps 串口连接、300 ms 心跳超时停车和断线后禁止自动恢复运动。
  - 7 月 20 日至 7 月 23 日
    - 编写 `SELF_CHECK`、`PATROL`、`CONFIRM`、`APPROACH`、`SCAN`、`DISPENSE`、`NOTIFY`、`RETURN` 和 `ERROR` 状态机。
    - 为每个状态定义进入动作、允许命令、退出条件、超时条件和失败去向。
    - 接入 DL-V1 的 `hand_raise` 事件、DL-V2 的 `card` 事件和巡线模块的 `line_error` 事件。
    - 丢弃时间戳超过 500 ms 的视觉事件，避免旧检测结果触发运动。
    - 实现教师协助请求的本地日志和通知；时间允许时增加 HTTP/SSE 教师端。
  - 7 月 24 日至 7 月 27 日
    - 汇总视觉 FPS、举手检出率、靠近成功率、返航成功率和出料成功率。
    - 切换目标后更新状态机配置、演示脚本和回归测试，不维护临时分叉代码。
    - 准备最终启动说明、冻结配置、运行日志、已知问题和故障处置单。
  - 主要交付物
    - 树莓派串口主机程序及串口协议文档。
    - 机器人任务状态机、事件接入层、能力开关配置和统一日志。
    - 教师通知功能、模拟事件工具、联调记录和演示运行单。
  - 协作关系
    - 与 RB-C 共同负责串口协议、心跳停车和运动命令联调。
    - 接收 DL-V1 和 DL-V2 的标准事件，但不修改其模型内部算法。
    - 与 RB-H 确认树莓派供电、相机连接和现场硬件状态。
  - 降级职责
    - 本机推理不稳定时，将推理位置切换到局域网 PC，同时保持事件格式不变。
    - 自动靠近不稳定时，将状态机切换为固定服务点模式。
    - 自动返航不稳定时，切换为限时搜线、停车告警和人工回线。

- **DL-V1：Deep Learning 姿态模型与目标方位负责人**
  - 核心职责
    - 负责 YOLOv8n-pose 部署、举手判定、时序确认、目标关联、方位计算和性能优化。
    - 向状态机输出标准化目标事件，不直接向 Arduino 下发电机命令。
  - 7 月 19 日至 7 月 21 日
    - 在开发机和树莓派上运行 YOLOv8n-pose，确认人体矩形框和 17 个关键点输出正确。
    - 测量推理模型推理延迟、内存占用等运行参数。
    - 实现摄像头连续取流和按目标推理频率抽帧。
    - 实现左手腕或右手腕高于鼻子/肩膀的举手规则，并加入人体框和关键点置信度阈值。
    - 输出 `track_id`、`target_x`、`distance_hint`、`conf` 和 `timestamp`。
  - 7 月 21 日至 7 月 23 日
    - 在 5 FPS 模式下实现最近 10 帧至少 7 帧命中的时序确认。
    - 在 3 FPS 模式下实现最近 6 帧至少 4 帧命中的备用时序确认。
    - 使用人体框 IoU（交并比，框之间的重合程度）或中心点最近邻（找下一帧人体框中与上一帧人体框欧氏距离最近的一对）关联目标，限制同一目标中心位置波动。
    - 实现超过 3 秒放弃目标的事件逻辑。
    - 完成前视相机水平角标定，根据目标在图片中的中心位置计算方位角。
    - 与 RB-C、DL-C 验证左、中、右目标的初始转向方向。
  - 7 月 24 日至 7 月 27 日
    - 与 DL-V2 根据场地数据调整关键点置信度、滑动窗口和位置一致性阈值。
    - 根据实际测试中出现的举手误触发、漏检、方位错误、目标丢失等问题，不断调整优化模型训练。
    - 决定树莓派从视频流抽帧处理的频率、资源卡形式、推理服务位于 PC 还是树莓派。
    - 冻结模型、推理后端、输入尺寸和阈值配置，进行真实场景测试。
  - 主要交付物
    - 可执行的举手检测服务和统一 `hand_raise` 事件接口。
    - 树莓派与 PC 推理性能基线报告。
    - 相机内参、水平角标定参数和目标方位测试记录。
    - 姿态 5 FPS、姿态 3 FPS 和颜色卡触发的兼容实现。
  - 协作关系
    - 与 DL-V2 共同完成举手和非举手数据评测及阈值选择。
    - 向 DL-C 提供标准事件和错误复现样本。
    - 与 RB-C 联合验证视觉方向输出与小车实际转向的一致性。
  - 降级职责
    - 树莓派低于 5 FPS 时先启用 3 FPS 时序规则或局域网 PC 推理。
    - 姿态检出率低于 80% 或误检率高于 10% 时，切换为指定颜色举手卡。
    - 所有降级模式继续输出相同的 `hand_raise` 字段。

- **DL-V2：Deep Learning 数据评测与请求卡负责人**
  - 核心职责
    - 负责目标教室数据采集、视觉指标评测、请求卡识别和视觉降级方案。
    - 使用测试数据支持模型阈值选择和 7 月 24 日的高低等级决断。
  - 7 月 19 日至 7 月 21 日
    - 设计举手和非举手测试清单，覆盖不同人员、距离、光照、坐姿、遮挡和相机高度。
    - 采集 10 至 15 人次的场地视频，并保留等量非举手负样本。
    - 根据预训练模型对于举手样本的识别准确率，决定是否对模型进行迁移训练。
    - 生成并打印两类资源 ArUco/二维码卡和一类教师协助卡。
    - 实现 OpenCV ArUco 或 QRCodeDetector（**比较两种方案确定选择**）请求卡识别并输出统一 `card` 事件。
  - 7 月 21 日至 7 月 23 日
    - 与 DL-V1 评测举手检出率、误检率、确认时间和目标位置稳定性。
    - 为每种请求卡完成 0.2 至 0.5 米、不同角度和不同光照条件下的测试。
    - 每类请求卡至少测试 30 次，目标为 3 秒内识别率达到 95%。
    - 实现请求卡 10 秒识别超时和重复识别去抖。
    - 开发指定黄色或其他约定颜色卡检测，作为姿态模型失败时的触发方案。
  - 7 月 24 日至 7 月 27 日
    - 总结举手、非举手和请求卡测试效果，为决策是否采用降级实现提供量化依据。
    - 确定使用的测试卡样式、卡与资源的映射关系、识别区范围（人与小车距离）。
    - 确定上述参数后，重新进行一次模型测试，验证模型识别能力，记录模型工作出错的场景（如多人举卡、距离过远、角度太大）。
  - 主要交付物
    - 场地举手/非举手数据集、测试清单和指标报告。
    - ArUco/二维码资产、请求卡检测服务和卡号映射。
    - 颜色卡降级检测器、阈值配置和视觉回归记录。
  - 协作关系
    - 与 DL-V1 共同决定姿态置信度、时序命中数和位置一致性阈值。
    - 向 DL-C 提供标准 `card` 事件和最终卡号定义。
    - 与 RB-H、RB-C 验证卡片识别距离和对应资源仓动作。
  - 降级职责
    - 多类请求卡不稳定时只保留一种资源卡。
    - 姿态检测不稳定时启用指定颜色举手卡并重新测试误触发率。
    - 所有卡片降级只调整配置和映射，不改变状态机接口。

- **RB-H：Robotics 硬件、场地与资源仓负责人**
  - 核心职责
    - 负责复核现有小车、安装新增器件、保障供电和机械安全、铺设巡游路线以及制作资源仓。
    - 负责联调和演示期间的现场硬件安全与备件管理。
    - 负责制作小组产品宣传物料。
  - 7 月 19 日至 7 月 20 日
    - 检查底盘、电机、驱动板、Arduino、电池、紧固件、轮胎和线缆状态。
    - 维护接线图，确认电机电源与逻辑电源分路并共地。
    - 安装并固定树莓派、前视相机、下视相机、超声波、急停、蜂鸣器、LED 和舵机。
    - 使前视相机尽量靠近底盘旋转中心线，保证视角、防震和线缆不干涉车轮。
    - 配合 RB-C 按供电、急停、空载电机、传感器、相机和舵机的顺序完成硬件自检。
  - 7 月 21 日至 7 月 23 日（*具体资源仓结构待进一步确定*）
    - 铺设无岔路的单向闭环色带，调整颜色、宽度、转弯半径和下视相机高度。
    - 确定真实场景中固定服务点、靠近测试区、障碍物和返航测试位置。
    - 制作双格舵机重力滑仓，调整仓门、导轨和物品尺寸。
    - 配合 RB-C 完成每个仓位的连续出料测试和舵机角度调整。
    - 在运动测试中检查轮胎打滑、车体偏重、传感器松动和供电压降。
  - 7 月 24 日至 7 月 27 日
    - 决定小车自带仓储的形态（无舵机外置篮筐、双仓、单仓等）。
    - 固定最终路线、服务点、摄像头位置和物料，不再进行高风险结构调整。
    - 准备备用电池、USB 线、杜邦线、胶带、卡片、紧固件和机械工具。
    - 在连续彩排和最终演示中负责硬件开机检查、急停和现场安全。
    - 设计小组海报样式，录制并制作演示视频链接二维码。
  - 主要交付物
    - 完整车体、接线图、器件清单和硬件自检表。
    - 单向闭环色带路线、服务点和演示场地。
    - 双格仓、单格仓或外置篮筐以及出料可靠性记录。
    - 备用电源、线材、卡片和机械备件清单。
    - 小组产品海报。
  - 协作关系
    - 与 RB-C 确认引脚、传感器位置、舵机角度和机械运动范围。
    - 与 DL-C 验证树莓派供电、USB 连接和摄像头设备稳定性。
    - 与 DL-V2 确认卡片尺寸、举卡距离和现场光照条件。
  - 降级职责
    - 下视相机巡线不稳定时安装或启用三路红外传感器。
    - 双格仓出料成功率低于 95%时切换单格仓，仍不稳定时改用外置篮筐。
    - 自动靠近不稳定时布置固定服务点并标记学生站位。

- **RB-C：Robotics Arduino 控制与运动负责人**
  - 核心职责
    - 负责 Arduino 固件、电机与传感器控制、串口从机端、巡线、靠近、返航和安全运动逻辑。
    - 负责维护所有运动参数并处理联调和演示中的运动故障。
  - 7 月 19 日至 7 月 20 日
    - 复核现有四轮电机控制代码，确认左右轮方向、PWM 范围和停车行为。
    - 实现或整理编码器计数、超声波采样、舵机、蜂鸣器、LED 和故障标志接口。
    - 与 DL-C 冻结串口协议，实现 `CMD`、`STOP`、`ACT` 的解析和 `STATE` 回传。
    - 实现序号处理、格式错误拒绝、300 ms 心跳超时停车和 0.3 米近障急停。
    - 完成低速前进、后退、原地转向、显式停车和执行器动作自检。
  - 7 月 21 日至 7 月 23 日（*资源仓部分待确定*）
    - 开发单环巡线控制，接收下视视觉偏差或三路红外传感器结果并调整左右轮速度。
    - 调整低速巡线 PID 或比例控制，至少连续完成 3 来回不丢线。
    - 根据 `target_x` 实现目标方向修正，并对角速度和转向时间设置上限。
    - 实现分级靠近速度、0.7 米服务停止和 30 秒靠近超时。
    - 记录离线时的编码器分段，执行逆序粗返航和最长 15 秒的色带搜索。
    - 实现舵机开仓、约 3 秒保持、关仓和声光提示动作。
  - 7 月 24 日至 7 月 27 日
    - 根据测试结果冻结 PWM、PID、超声波阈值、舵机角度和所有运动超时参数。
    - 自动靠近未达到 8/10 时切换固定服务点，不继续增加复杂控制逻辑。
    - 自动返航未达到 8/10 时保留限时搜线和停车告警，由人工完成回线。
    - 多次测试靠近、返航、断联、近障和急停等场景下小车工作可靠性。
  - 主要交付物
    - Arduino 固件、引脚定义和串口从机协议实现。
    - 电机、编码器、超声波、舵机、蜂鸣器和 LED 的自检程序或测试入口。
    - 巡线、靠近、返航和安全停车控制逻辑。
    - PWM、PID、距离、舵机角度和超时参数表及运动测试记录。
  - 协作关系
    - 与 RB-H 共同解决供电、传感器安装、轮胎打滑和资源仓机械问题。
    - 与 DL-C 共同验证串口、心跳、状态机命令和失联停车。
    - 与 DL-V1 验证视觉方位和实际转向方向，与 DL-V2 验证卡号和执行动作。
  - 降级职责
    - 下视视觉巡线不稳定时切换为三路红外单环巡线。
    - 轮速闭环来不及稳定时使用经过测试的低速开环 PWM。
    - 靠近或返航不稳定时关闭对应自动能力，但始终保留急停、近障停车和运动超时。

关键时间节点
- 7 月 23 日：基础功能验证，全流程基本跑通，总结串联过程中仍然存在的问题。
- 7 月 24 日：分析问题规模并尝试解决，决定每个模块采用期望目标还是降级目标。
- 7 月 26 日：完成实操演示、异常工况测试。
